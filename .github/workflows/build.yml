name: build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:

  build_CPU:

    name: ${{ matrix.platform }}, py${{ matrix.python-version }}, ${{ matrix.extra }}
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        platform: [
          # ubuntu-latest,
          ubuntu-22.04,
          # ubuntu-20.04,
          # ubuntu-18.04,
          # # windows-latest,
          # windows-2022,
          # windows-2019,
          # windows-2016,
          # # macos-latest,
          # macos-12.0,
          # macos-11.0,
          # macos-10.15,
        ]
        python-version: [ 
          # "3.8",
          "3.9",
          # "3.10",
        ]
        extra: [
          core,
          core_latest,
        ]

    steps:
    
    - name: Set up conda
      uses: conda-incubator/setup-miniconda@v2
      with:
        miniconda-version: latest
        activate-environment: ROICaT
        auto-activate-base: true
        auto-update-conda: false
        remove-profiles: true
        architecture: x64
        clean-patched-environment-file: true
        run-post: true

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}

    - name: Check out repository code
      uses: actions/checkout@v3
        
    - name: PowerShell Core
      shell: pwsh
      run: |
          conda info
          conda list
    
    - name: Prepare path, environment, and basic pip packages
      run: |
        ## cd to github workspace
        # cd $GITHUB_WORKSPACE
        
        ## check current directory (pwd)
        pwd

        ## activate conda environment        
        conda activate ROICaT || true

        ## install pip, wheel, setuptools, tox
        python -m pip install --upgrade pip setuptools wheel pytest tox tox-gh-actions

    - name: Install ROICaT with pip dependencies
      run: |
        pip install --user -v -e .[${{ matrix.extra }}]
        if [ "${{ matrix.extra }}" == "core_latest" ]; then
          pip install torch torchvision torchaudio -f https://download.pytorch.org/whl/torch_stable.html
        fi
        ## If matrix.extra == 'core' and the OS is linux or windows (avoid using matrix.platform)
        if [ "${{ matrix.extra }}" == "core" ] && ([ "${{ runner.os }}" == "Linux" ] || [ "${{ runner.os }}" == "Windows" ]); then
          pip install torch==1.12.1+cpu torchvision==0.13.1+cpu torchaudio==0.12.1 -f https://download.pytorch.org/whl/torch_stable.html
        fi
        if [ "${{ matrix.extra }}" == "core" ] && [ "${{ runner.os }}" == "macOS" ]; then
          pip install torch==1.12.1 torchvision==0.13.1 torchaudio==0.12.1 -f https://download.pytorch.org/whl/torch_stable.html
        fi


    - name: Run pytest on ROICaT and some key dependencies
      run: |
        pip install tox pytest
        pytest -v
